#!/bin/sh

# Notes -- an ad-hoc Bash/Git journal system

set -e

source ~/.profile

NOW=`date +%Y-%m-%d`

proj
cd notes

git pull

touch entries/$NOW.md
subl --wait entries/$NOW.md

git add entries/$NOW.md
git commit -m "Update note $NOW"
git push